# Wait for cloud-init to complete before starting Docker
# This ensures CA certificates are installed before Docker daemon loads them
# Without this, Docker would start with the old CA bundle and not trust our registry cache CA
[Unit]
After=cloud-final.service
