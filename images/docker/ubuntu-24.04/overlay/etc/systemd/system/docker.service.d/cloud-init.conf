# Wait for cloud-init config stage before starting Docker
# CA cert is written in init stage (write_files), updated in config stage (runcmd)
# Note: Can't use cloud-final.service - it runs AFTER multi-user.target, causing a cycle
# cloud-config.service is when update-ca-certificates runs
[Unit]
After=cloud-config.service
